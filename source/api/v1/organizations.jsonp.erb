callback(<%=
  ([{ name: "None", value: "" }] + find_organizations.map { |o| {
    name: o.data.title,
    value: o.path.sub(/\.html$/, '').split('/').last,
    links: {
    	html: url_for(o),
    	website: o.data.url,
    	logo: o.data.logo
    },
    description: o.render({:layout => false}).split(/<\/?h2.*?>/).first
  }}.sort_by { |o| o[:name] }).to_json
%>)